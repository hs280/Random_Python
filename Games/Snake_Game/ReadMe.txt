Engineering Science
First Year Computing Laboratory
Snake Game
To play run snake_game_main.m
answer questions then use arrow keys to control movement
To change presets edit snake_game.m
reccomended to edit maxsize and maxdiff based on hardware specifications
When length is greater than max length then a moving sprite will spawn this sprite needs to be captured after which lenth will reset to 2 and difficulty will increase by 1
if diff =max diff only the creatrure will spawn