Asteroid style game

Block generation done.

improve movementny moving bexier rather tahn by moving sprite and changing axis i.e. keep axis constant
Collision detect by seeeing if point is in P_cell Store P_cell for current Cell and next 
make it so that it moves up with speed and you can move left and right
collision detect by seeing if point is in P_cell_current
Give score based on current y_value
if hit edges in x die 
reuse snake code where neccesary 
addd timer 